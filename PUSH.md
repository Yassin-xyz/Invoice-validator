# Publier sur votre GitHub

1. Créez un dépôt vide sur github.com (ex. `facture-schematron-validator`), sans README.
2. Puis :

```bash
git remote add origin https://github.com/VOTRE-COMPTE/facture-schematron-validator.git
git branch -M main
git push -u origin main
```
