{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Lancer l'extension",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "preLaunchTask": "npm: compile"
    }
  ]
}
